// src/pages/SignupPage.tsx
import SignupForm from '../components/auth/SignupForm'; // Import the form

const SignupPage = () => {
  return (
    <div>
      {/* We might add headings or links here later if needed */}
      <SignupForm />
    </div>
  );
};

export default SignupPage;